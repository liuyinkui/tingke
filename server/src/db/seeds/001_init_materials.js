/**
 * Seed — 初始素材数据
 *
 * 70 篇素材将在 Issue #18 中填充。
 * 当前为空壳，确保 migrate 后可以 rolling forward。
 */
exports.seed = async function (_knex) {
  // No data yet — awaiting seed data pipeline
  console.log('[Seed] 空壳 seed 已运行，等待 Issue #18 填充数据');
};
